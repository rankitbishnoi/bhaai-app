import {StyleSheet} from 'react-native';

export default function useStyles() {
  return StyleSheet.create({
    container: {
      position: 'absolute',
      start: 16,
      end: 16,
      bottom: 16,
    },
  });
}
